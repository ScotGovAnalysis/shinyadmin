#' Microsoft Form Names
#'
#' A lookup dataset for renaming columns in the Microsoft Forms contact dataset.
#' Contains two columns: `new` and `old`. The `old` column should match the
#' question wording in the MS form exactly. The `new` column contains clean
#' snakecase names to use in the analysis dataset.

"ms_form_names"
